# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Adverts resource
GET     /adverts             controllers.CarAdvertController.index
POST    /adverts             controllers.CarAdvertController.create
GET     /adverts/:id         controllers.CarAdvertController.get(id: Int)